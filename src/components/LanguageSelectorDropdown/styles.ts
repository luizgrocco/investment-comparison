import styled from "styled-components";
import { Grid } from "@mui/material";
export {
  Select as SLanguageDropdown,
  MenuItem as SLanguageItem,
  InputLabel as SLanguageLabel,
  FormControl as SFormControl,
} from "@mui/material";

export const SLanguageSelectorContainer = styled(Grid)``;

// FIXME: Dark mode text color is impossible to read
